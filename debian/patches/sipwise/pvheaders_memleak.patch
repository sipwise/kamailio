From: Sipwise Development Team <support@sipwise.com>
Date: Tue, 4 Aug 2026 11:24:57 +0200
Subject: pvheaders_memleak

---
 src/modules/pv_headers/pvh_xavp.c | 15 ++++++++++++++-
 1 file changed, 14 insertions(+), 1 deletion(-)

diff --git a/src/modules/pv_headers/pvh_xavp.c b/src/modules/pv_headers/pvh_xavp.c
index f367588..cef6a08 100644
--- a/src/modules/pv_headers/pvh_xavp.c
+++ b/src/modules/pv_headers/pvh_xavp.c
@@ -711,7 +711,13 @@ xavp_c_data_t *pvh_set_parsed(
 	return c_data;
 
 err:
-	// how can I call?? pvh_xavi_free_data(c_data, shm_free);
+	if(c_data != NULL) {
+		if(c_data->value.s != NULL) {
+			shm_free(c_data->value.s);
+		}
+		shm_free(c_data);
+		c_data = NULL;
+	}
 	return NULL;
 }
 
@@ -884,6 +890,13 @@ int pvh_set_uri(struct sip_msg *msg, pv_param_t *param, int op, pv_value_t *val)
 err:
 	if(pv_format)
 		pv_elem_free_all(pv_format);
+	if(c_data != NULL) {
+		if(c_data->value.s != NULL) {
+			shm_free(c_data->value.s);
+		}
+		shm_free(c_data);
+		c_data = NULL;
+	}
 	return -1;
 }
 
