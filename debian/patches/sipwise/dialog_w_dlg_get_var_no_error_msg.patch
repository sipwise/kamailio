From: Sipwise Development Team <support@sipwise.com>
Date: Tue, 4 Aug 2026 11:24:57 +0200
Subject: dialog_w_dlg_get_var_no_error_msg

---
 src/modules/dialog/dialog.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/src/modules/dialog/dialog.c b/src/modules/dialog/dialog.c
index 5dfdec1..b26fb0f 100644
--- a/src/modules/dialog/dialog.c
+++ b/src/modules/dialog/dialog.c
@@ -2081,7 +2081,7 @@ static int w_dlg_get_ttag(struct sip_msg *msg, char *ci, char *ft, char *pv)
 		pv_get_null(msg, NULL, &dst_val);
 	}
 	if(pv_set_spec_value(msg, dst_pv, 0, &dst_val) != 0) {
-		LM_ERR("unable to set value to dst_pv\n");
+		LM_INFO("unable to set value to dst_pv\n");
 		if(val) goto error; else return -1;
 	}
 	return 1;
@@ -2089,7 +2089,7 @@ static int w_dlg_get_ttag(struct sip_msg *msg, char *ci, char *ft, char *pv)
 error:
 	pv_get_null(msg, NULL, &dst_val);
 	if(pv_set_spec_value(msg, dst_pv, 0, &dst_val) != 0) {
-		LM_ERR("unable to set null value to dst_pv\n");
+		LM_INFO("unable to set null value to dst_pv\n");
 	}
 	return -1;
 }
